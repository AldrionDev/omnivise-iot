import { EmptyState } from '../components/EmptyState'

/** Placeholder -- real content lands in #75/#76. */
export function DevicesPage() {
  return <EmptyState title="Devices" description="The device registry view is coming in a later issue." />
}
