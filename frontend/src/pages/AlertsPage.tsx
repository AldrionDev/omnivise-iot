import { EmptyState } from '../components/EmptyState'

/** Placeholder -- real content lands in #76. */
export function AlertsPage() {
  return <EmptyState title="Alerts" description="The alert history view is coming in a later issue." />
}
