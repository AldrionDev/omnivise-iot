import { EmptyState } from '../components/EmptyState'

/** Placeholder -- real content lands in #75. */
export function OverviewPage() {
  return <EmptyState title="Overview" description="Fleet overview is coming in a later issue." />
}
